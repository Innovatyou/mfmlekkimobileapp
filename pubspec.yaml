name: higherground
description: Higher Ground Mobile Application

# The following line prevents the package from being accidentally published to
# pub.dev using `pub publish`. This is preferred for private packages.
publish_to: "none" # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
version: 1.0.0+20

environment:
  sdk: ">=3.3.0 <4.0.0"


dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  # Core
  intl: any
  provider: ^6.1.2
  dio: ^5.4.0
  http: ^1.2.1
  path: ^1.9.0
  collection: ^1.18.0

  # SQLite
  sqflite: ^2.4.2

  # Media / Audio / Video
  better_player_plus: ^1.0.8
  better_player:
    path: packages/better_player
  video_player: any
  chewie: any
  # removed local shim; use `better_player_plus` implementation
  just_audio: ^0.9.37

  # Content & UI
  syncfusion_flutter_pdfviewer: ^31.1.21
  cached_network_image: ^3.3.1
  shimmer: ^3.0.0
  lottie: ^3.1.0
  carousel_slider: ^4.2.1
  table_calendar: ^3.1.2
  expandable: ^5.0.1
  badges: ^3.1.2

  # Firebase
  firebase_core: ^4.13.0
  firebase_messaging: ^16.5.0
  firebase_analytics: ^12.4.6

  # Storage / Permissions
  shared_preferences: ^2.2.3
  path_provider: ^2.1.3
  permission_handler: ^11.3.0
  file_picker: ^8.0.5

  # Utilities
  url_launcher: ^6.2.6
  event_bus: ^2.0.0
  package_info_plus: ^8.3.1
  flutter_secure_storage: ^9.0.0
  visibility_detector: ^0.4.0+2

  cupertino_icons: ^1.0.8
  
  # Restored UI & utilities (required by existing code)
  share_plus: ^7.2.2
  flutter_material_color_picker: ^1.1.0
  select_dialog: ^2.0.1
  highlight_text: ^1.6.0
  flutter_widget_from_html: ^0.15.2
  # Pinned to a commit past 11.5.1 (the latest pub.dev release): fixes
  # missing TextInputClient.onFocusReceived, required by this Flutter SDK
  # (3.44.4) and merged upstream (PR #2726) but not yet published as a
  # release. Switch back to a version constraint once a release picks it up.
  flutter_quill:
    git:
      url: https://github.com/singerdmx/flutter-quill.git
      ref: 1643f56b51a8f2442c63863dde7584a61fe222a9
  flutter_login: any
  pull_to_refresh: ^2.0.0
  line_awesome_flutter: ^2.0.0
  smooth_page_indicator: ^1.0.1
  # flutter_sticky_header removed — replaced by simple sectioned lists
  youtube_player_iframe: any
  gallery_image_viewer: ^1.2.0
  isolated_download_manager: ^1.0.2
  font_awesome_flutter: ^11.0.0
  clipboard: ^0.1.3
  # removed flutter_absolute_path (replaced by using File.path)
  photo_view: ^0.14.0
  in_app_review: ^2.0.9
  image: ^4.0.0
  flutter_snake_navigationbar: any
  flutter_local_notifications: any
  flutter_web_browser: any
  webview_flutter: ^4.8.0
  flash: any
  floating: ^6.0.0
  # translator removed: replaced by internal/no-op translator to avoid package URI issues
  # maps_launcher removed: replaced with URL open via Utility.openBrowserTab

dependency_overrides:
  wakelock: any
  flutter_inappwebview: ^6.1.5
  flutter_inappwebview_platform_interface: 1.2.0

dev_dependencies:
  build_runner: ^2.1.10
  flutter_launcher_icons: ^0.13.1
  flutter_native_splash: ^2.4.7

flutter_icons:
  android: "launcher_icon"
  ios: true
  remove_alpha_ios: true
  image_path: "assets/icon/icon.png"

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter.
flutter:
  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  assets:
    - assets/icon/
    - assets/images/
    - assets/lottie/
    - assets/sounds/
    - assets/splash/

  fonts:
    - family: airbnb
      fonts:
        - asset: assets/fonts/NexaRegular.otf
        - asset: assets/fonts/NexaBlack.otf
          weight: 500
        - asset: assets/fonts/NexaXBold.otf
          weight: 600

  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/custom-fonts/#from-packages

flutter_native_splash:
  # This package generates native code to customize Flutter's default white native splash screen
  # with background color and splash image.
  # Customize the parameters below, and run the following command in the terminal:
  # To generate splash assets, run:
  # dart run flutter_native_splash:create
  # To restore Flutter's default white splash screen, run the following command in the terminal:
  # dart run flutter_native_splash:remove

  # color or background_image is the only required parameter.  Use color to set the background
  # of your splash screen to a solid color.  Use background_image to set the background of your
  # splash screen to a png image.  This is useful for gradients. The image will be stretch to the
  # size of the app. Only one parameter can be used, color and background_image cannot both be set.
  #color: "#42a5f5"
  color: "#A13C76"

  # Optional parameters are listed below.  To enable a parameter, uncomment the line by removing
  # the leading # character.

  # The image parameter allows you to specify an image used in the splash screen.  It must be a
  # png file and should be sized for 4x pixel density.
  color_dark: "#6F174D"
  android_12:
    color: "#A13C76"
    color_dark: "#6F174D"
  web: false

  # This property allows you to specify an image used as branding in the splash screen. It must be
  # a png file. Currently, it is only supported for Android and iOS.
  #branding: assets/dart.png

  # Specify your branding image for dark mode.
  #branding_dark: assets/dart_dark.png

  # To position the branding image at the bottom of the screen you can use bottom, bottomRight,
  # and bottomLeft. The default values is bottom if not specified or specified something else.
  #
  # Make sure this content mode value should not be similar to android_gravity value and
  # ios_content_mode value.
  #branding_mode: bottom

  # The color_dark, background_image_dark, and image_dark are parameters that set the background
  # and image when the device is in dark mode. If they are not specified, the app will use the
  # parameters from above. If the image_dark parameter is specified, color_dark or
  # background_image_dark must be specified.  color_dark and background_image_dark cannot both be
  # set.
  #color_dark: "#042a49"
  #background_image_dark: "assets/dark-background.png"
  #image_dark: assets/splash-invert.png

  # The android, ios and web parameters can be used to disable generating a splash screen on a given
  # platform.
  #android: false
  #ios: false
  #web: false

  # The position of the splash image can be set with android_gravity, ios_content_mode, and
  # web_image_mode parameters.  All default to center.
  #
  # android_gravity can be one of the following Android Gravity (see
  # https://developer.android.com/reference/android/view/Gravity): bottom, center,
  # center_horizontal, center_vertical, clip_horizontal, clip_vertical, end, fill, fill_horizontal,
  # fill_vertical, left, right, start, or top.
  #android_gravity: center
  #
  # ios_content_mode can be one of the following iOS UIView.ContentMode (see
  # https://developer.apple.com/documentation/uikit/uiview/contentmode): scaleToFill,
  # scaleAspectFit, scaleAspectFill, center, top, bottom, left, right, topLeft, topRight,
  # bottomLeft, or bottomRight.
  #ios_content_mode: center
  #
  # web_image_mode can be one of the following modes: center, contain, stretch, and cover.
  #web_image_mode: center

  # To hide the notification bar, use the fullscreen parameter.  Has no effect in web since web
  # has no notification bar.  Defaults to false.
  # NOTE: Unlike Android, iOS will not automatically show the notification bar when the app loads.
  #       To show the notification bar, add the following code to your Flutter app:
  #       WidgetsFlutterBinding.ensureInitialized();
  #       SystemChrome.setEnabledSystemUIOverlays([SystemUiOverlay.bottom, SystemUiOverlay.top]);
  #fullscreen: true

  # If you have changed the name(s) of your info.plist file(s), you can specify the filename(s)
  # with the info_plist_files parameter.  Remove only the # characters in the three lines below,
  # do not remove any spaces:
  #info_plist_files:
  #  - 'ios/Runner/Info-Debug.plist'
  #  - 'ios/Runner/Info-Release.plist'
